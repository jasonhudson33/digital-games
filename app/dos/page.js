import DosClient from "../../components/dos-client";

export const metadata = { title: "DOS | Digital Games", description: "Play classic DOS in private rooms with friends and computer players." };

export default function DosPage() { return <DosClient />; }
